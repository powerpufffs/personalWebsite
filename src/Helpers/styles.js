import { css } from '@emotion/core'

export const setup = css`
  margin: 0;
  padding: 0;
`
export const overrideLinks = css`
  text-decoration: none;
  color: inherit;
  border: none;
  background-color: transparent;
`
